#include <SFML/Graphics.hpp>
#include <cmath>
#include <vector>
#include <iostream>

int main()
{
	 sf::RenderWindow window(sf::VideoMode(800, 600), "My window");

	 while (window.isOpen())
	 {
	 }

	return 0;
}